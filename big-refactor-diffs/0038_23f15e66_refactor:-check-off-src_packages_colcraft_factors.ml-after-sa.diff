diff --git a/refactor/checklist.md b/refactor/checklist.md
index 1a593361..b9b0335e 100644
--- a/refactor/checklist.md
+++ b/refactor/checklist.md
@@ -49,7 +49,7 @@
 | `src/packages/colcraft/distinct.ml` | [x] |
 | `src/packages/colcraft/drop_na.ml` | [x] |
 | `src/packages/colcraft/expand.ml` | [x] |
-| `src/packages/colcraft/factors.ml` | [ ] |
+| `src/packages/colcraft/factors.ml` | [x] |
 | `src/packages/colcraft/fill.ml` | [ ] |
 | `src/packages/colcraft/group_by.ml` | [ ] |
 | `src/packages/colcraft/joins.ml` | [ ] |
diff --git a/src/packages/colcraft/factors.ml b/src/packages/colcraft/factors.ml
index 24ebe0fc..3446cc66 100644
--- a/src/packages/colcraft/factors.ml
+++ b/src/packages/colcraft/factors.ml
@@ -205,7 +205,7 @@ let fct_recode_impl (args : (string option * value) list) _env =
             
             let factor_arr = Array.map (function
               | VFactor (i, _, _) ->
-                  let new_idx = level_remapping.(i) in
+                  let new_idx = if i >= 0 && i < Array.length level_remapping then level_remapping.(i) else 0 in
                   VFactor (new_idx, final_levels, ordered)
               | v -> v
             ) arr in
@@ -230,10 +230,11 @@ let fct_reorder_impl (args : (string option * value) list) _env =
              for i = 0 to n - 1 do
                match f_arr.(i) with
                | VFactor (idx, _, _) -> 
-                   (match x_arr.(i) with
-                    | VInt x -> level_data.(idx) <- float_of_int x :: level_data.(idx)
-                    | VFloat x -> level_data.(idx) <- x :: level_data.(idx)
-                    | _ -> ())
+                   if idx >= 0 && idx < Array.length level_data then
+                     (match x_arr.(i) with
+                      | VInt x -> level_data.(idx) <- float_of_int x :: level_data.(idx)
+                      | VFloat x -> level_data.(idx) <- x :: level_data.(idx)
+                      | _ -> ())
                | _ -> ()
              done;
              
@@ -257,10 +258,12 @@ let fct_reorder_impl (args : (string option * value) list) _env =
              let new_level_order = List.map fst sorted_summaries in
              let new_levels = List.map (fun i -> List.nth levels i) new_level_order in
              let remapping = Array.make (List.length levels) 0 in
-             List.iteri (fun i old_idx -> remapping.(old_idx) <- i) new_level_order;
+             List.iteri (fun i old_idx -> if old_idx >= 0 && old_idx < Array.length remapping then remapping.(old_idx) <- i) new_level_order;
              
              let factor_arr = Array.map (function
-               | VFactor (i, _, _) -> VFactor (remapping.(i), new_levels, ordered)
+               | VFactor (i, _, _) ->
+                   let new_idx = if i >= 0 && i < Array.length remapping then remapping.(i) else 0 in
+                   VFactor (new_idx, new_levels, ordered)
                | v -> v
              ) f_arr in
              VVector factor_arr
@@ -297,10 +300,12 @@ let fct_lump_n_impl (args : (string option * value) list) _env =
              let other_idx = List.length new_levels - 1 in
              
              let remapping = Array.make (List.length levels) other_idx in
-             List.iteri (fun i old_idx -> remapping.(old_idx) <- i) top_indices;
+             List.iteri (fun i old_idx -> if old_idx >= 0 && old_idx < Array.length remapping then remapping.(old_idx) <- i) top_indices;
              
              let factor_arr = Array.map (function
-               | VFactor (i, _, _) -> VFactor (remapping.(i), new_levels, ordered)
+               | VFactor (i, _, _) ->
+                   let new_idx = if i >= 0 && i < Array.length remapping then remapping.(i) else other_idx in
+                   VFactor (new_idx, new_levels, ordered)
                | v -> v
              ) arr in
              VVector factor_arr
@@ -332,13 +337,16 @@ let fct_relevel_impl (args : (string option * value) list) _env =
              
              let remapping = Array.make (List.length levels) 0 in
              List.iteri (fun i old_l ->
-               match List.find_index (fun l -> l = old_l) new_levels with
-               | Some idx -> remapping.(i) <- idx
-               | None -> ()
+               if i >= 0 && i < Array.length remapping then
+                 match List.find_index (fun l -> l = old_l) new_levels with
+                 | Some idx -> remapping.(i) <- idx
+                 | None -> ()
              ) levels;
              
              let factor_arr = Array.map (function
-               | VFactor (i, _, _) -> VFactor (remapping.(i), new_levels, ordered)
+               | VFactor (i, _, _) ->
+                   let new_idx = if i >= 0 && i < Array.length remapping then remapping.(i) else 0 in
+                   VFactor (new_idx, new_levels, ordered)
                | v -> v
              ) f_arr in
              VVector factor_arr
@@ -387,7 +395,9 @@ let fct_collapse_impl (args : (string option * value) list) _env =
              ) levels |> Array.of_list in
              
              let factor_arr = Array.map (function
-               | VFactor (i, _, _) -> VFactor (level_remapping.(i), final_levels, ordered)
+               | VFactor (i, _, _) ->
+                   let new_idx = if i >= 0 && i < Array.length level_remapping then level_remapping.(i) else 0 in
+                   VFactor (new_idx, final_levels, ordered)
                | v -> v
              ) arr in
              VVector factor_arr
