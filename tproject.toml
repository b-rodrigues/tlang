[project]
name = "tlang"
version = "0.52.0"

[r-dependencies]
packages = ["dplyr", "readr", "testthat"]

[py-dependencies]
packages = ["pandas", "numpy"]

[julia-dependencies]
version = "lts"
packages = ["CSV", "DataFrames", "JSON"]
