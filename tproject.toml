[project]
name = "tlang"

[py-dependencies]
packages = ["pandas", "scikit-learn", "statsmodels", "patsy", "sklearn2pmml"]

[r-dependencies]
packages = ["dplyr", "faraway", "MASS", "glmnet", "r2pmml"]
