[project]
name = "tlang"
version = "0.51.4"

[r-dependencies]
packages = ["dplyr", "readr", "testthat"]

[py-dependencies]
packages = ["pandas", "numpy"]

[julia-dependencies]
version = "lts"
packages = ["CSV", "DataFrames", "JSON"]
