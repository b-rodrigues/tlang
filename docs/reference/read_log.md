# read_log

Read Build Log

Parses a JSON build log to extract node names and their artifact paths.

## Parameters

- **path** (`String`): Path to the JSON log file.

## Returns

String)], String] List of (node, path) pairs.

