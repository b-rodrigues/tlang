# unparse_expr

Expression Unparser (Nix context)

Converts T expressions back into a string representation suitable for embedding in a Nix build script.

## Parameters

- **expr** (`Expression`): The T expression to unparse.

## Returns

The string representation.

