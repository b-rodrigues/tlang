[package]
name = "test-pkg"
version = "0.1.0"
description = "A brief description of what test-pkg does"
authors = ["Your Name <email@example.com>"]
license = "EUPL-1.2"
homepage = ""
repository = ""

[dependencies]
# T packages this package depends on
# Format: package = { git = "repository-url", tag = "version" }

[t]
# Minimum T language version required
min_version = "0.5.0"
