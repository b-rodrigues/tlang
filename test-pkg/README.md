# test-pkg

A T package.

## Description

A brief description of what test-pkg does.

## Installation

Add the following to the `[dependencies]` section of your `tproject.toml`:

```toml
[dependencies]
test-pkg = { git = "https://github.com/username/test-pkg", tag = "v0.1.0" }
```

Then run `nix develop` to enter the environment with the package available.

## Usage

```t
-- Example usage of test-pkg functions
```

## Development

Enter the development shell:

```bash
nix develop
```

Run tests:

```bash
t test
```

## License

EUPL-1.2
